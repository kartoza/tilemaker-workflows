services:
  tilemaker-server:
    image: ${TILEMAKER_IMAGE:-tilemaker-server:latest}
    container_name: tilemaker-server
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./output:/data:ro
    environment:
      - DATA_DIR=/data
